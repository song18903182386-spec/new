<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>学习笔记 | 宋杨</title>
    <style>
        :root {
            --main-color: #a78bfa;
            --light-color: #c4b5fd;
            --bg-dark: #0f1f1e;
            --text-main: #f5f3ff;
            --text-gray: #a8a29e;
            --card-bg: rgba(255,255,255,0.07);
            --section-bg: rgba(255,255,255,0.04);
        }

        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background: rgba(15, 15, 30, 0.9);
            backdrop-filter: blur(12px);
            z-index: 999;
            padding: 15px 0;
            text-align: center;
            border-bottom: 1px solid rgba(167,139,250,0.2);
        }
        .navbar a {
            margin: 0 12px;
            color: var(--text-main);
            font-size: 0.95rem;
            transition: 0.3s;
            text-decoration: none;
        }
        .navbar a:hover {
            color: var(--main-color);
        }

        ::selection {
            background: var(--main-color);
            color: var(--bg-dark);
        }

        #starfield{
            position:fixed;
            top:0;
            left:0;
            width:100%;
            height:100%;
            z-index:0;
            pointer-events:none;
            background: #0f0f1e;
        }

        body {
            font-family: "Microsoft YaHei", "PingFang SC", sans-serif;
            margin: 0;
            padding: 0;
            background: #0f0f1e;
            min-height: 100vh;
            color: var(--text-main);
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        .container {
            max-width: 900px;
            margin: 100px auto 40px;
            padding: 40px;
            text-align: center;
            background: var(--card-bg);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.4);
            border: 1px solid rgba(255,255,255,0.15);
            position: relative;
            z-index: 1;
            animation: fadeIn 1s ease forwards;
            opacity: 0;
            transform: translateY(20px);
        }

        @keyframes fadeIn {
            to { opacity:1; transform:translateY(0); }
        }

        h1 {
            color: var(--main-color);
            font-size: 2.2rem;
            margin-bottom: 10px;
        }

        .desc {
            color: var(--text-gray);
            margin-bottom: 40px;
        }

        .note-item {
            background: var(--section-bg);
            padding: 20px 25px;
            border-radius: 16px;
            margin-bottom: 20px;
            text-align: left;
            transition: 0.3s;
            border-left: 3px solid var(--main-color);
        }

        .note-item:hover {
            transform: translateX(6px);
            background: rgba(255,255,255,0.08);
        }

        .note-date {
            color: var(--light-color);
            font-size: 0.85rem;
            margin-bottom: 6px;
        }

        .note-title {
            color: var(--text-main);
            font-size: 1.1rem;
            margin: 0 0 6px 0;
        }

        .note-content {
            color: var(--text-gray);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        small {
            color: var(--text-gray);
        }

        @media (max-width:768px) {
            .container {
                margin:100px 20px 40px;
                padding:25px;
            }
            h1 {
                font-size:1.8rem;
            }
            .navbar a {
                margin:0 6px;
                font-size:0.85rem;
            }
        }
    </style>
</head>

<body>

<!-- 导航栏 -->
<div class="navbar">
    <a href="index.html">首页</a>
    <a href="xuexi.html">学习笔记</a>
    <a href="project.html">项目作品</a>
    <a href="tools.html">AI工具</a>
    <a href="links.html">资源导航</a>
    <a href="about.html">关于我</a>
</div>

<!-- 星空背景 -->
<canvas id="starfield"></canvas>

<!-- 内容 -->
<div class="container">
    <h1>学习笔记 / Learning Notes</h1>
    <p class="desc">记录AI学习、成长思考、实践过程与收获</p>

    <!-- 学习笔记列表 -->
    <div class="note-item">
        <div class="note-date">2026-01-01</div>
        <h3 class="note-title">GitHub Pages 搭建个人网站完成</h3>
        <div class="note-content">成功搭建个人主页，实现星空背景、流星动画、响应式布局、中英双语展示，正式开启个人数字空间。</div>
    </div>

    <div class="note-item">
        <div class="note-date">2026-01-02</div>
        <h3 class="note-title">AI 工具在工作中的应用思路</h3>
        <div class="note-content">开始探索AI在品牌设计、文案创作、效率提升中的实际应用，逐步建立自己的工具库与工作流。</div>
    </div>

    <div class="note-item">
        <div class="note-date">2026-01-03</div>
        <h3 class="note-title">持续学习：终身成长的核心</h3>
        <div class="note-content">无论行业如何变化，保持学习习惯，才能不断发现新机会、新方向、新可能。</div>
    </div>

    <div class="note-item">
        <div class="note-date">持续更新中……</div>
        <h3 class="note-title">更多学习记录正在整理</h3>
        <div class="note-content">未来会持续记录AI学习、项目实践、工具测评、成长思考等内容。</div>
    </div>

    <br><br>
    <small>© 2026 Song Yang<br>Built with GitHub Pages & AI</small>
</div>

<script>
    const canvas=document.getElementById('starfield');
    const ctx=canvas.getContext('2d');
    let stars=[],meteors=[],w,h,lastTime=0;
    const FPS=60,interval=1000/FPS;

    function resize(){
        w=canvas.width=window.innerWidth;
        h=canvas.height=window.innerHeight;
        createStars();
    }
    window.addEventListener('resize',resize);resize();

    function createStars(){
        stars=[];
        for(let i=0;i<220;i++){
            stars.push({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.5+0.3,v:Math.random()*0.2+0.04});
        }
    }

    function createMeteor(){
        if(meteors.length<3){
            meteors.push({x:Math.random()*w,y:0,len:Math.random()*80+50,vx:Math.random()*6+4,vy:Math.random()*4+3,op:1});
        }
    }
    setInterval(createMeteor,2000);

    function draw(t){
        requestAnimationFrame(draw);
        if(t-lastTime<interval)return;lastTime=t;
        ctx.clearRect(0,0,w,h);

        ctx.fillStyle='#fff';
        stars.forEach(s=>{
            ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill();
            s.y+=s.v;if(s.y>h){s.y=0;s.x=Math.random()*w;}
        });

        ctx.strokeStyle='rgba(167,139,250,1)';ctx.lineWidth=2;
        meteors.forEach((m,i)=>{
            ctx.beginPath();ctx.moveTo(m.x,m.y);ctx.lineTo(m.x-m.len,m.y-m.len);ctx.stroke();
            m.x+=m.vx;m.y+=m.vy;m.op-=0.01;
            if(m.y>h||m.op<=0)meteors.splice(i,1);
        });
    }
    draw(0);
</script>

</body>
</html>
